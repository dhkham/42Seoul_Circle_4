{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug using LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/cub3D", 
            "args": ["map.cub"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
